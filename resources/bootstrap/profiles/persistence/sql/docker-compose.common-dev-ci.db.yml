services:
  db:
    image: postgres:{{}}
    environment:
      - POSTGRES_HOST
      - POSTGRES_PORT
      - POSTGRES_DB
      - POSTGRES_USER
      - POSTGRES_PASSWORD
  ports:
    - "5432:5432"
  volumes:
      - ./postgres:/docker-entrypoint-initdb.d
