services:
  keycloak:
    volumes:
      - "/var/app/current/keycloak/themes/{{project.profiles.auth-keycloak.to-deploy.container.theme.name}}:/opt/jboss/keycloak/themes/{{project.profiles.auth-keycloak.to-deploy.container.theme.name}}:ro"
